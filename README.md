# passwordGeneratorXKCD


Creates a pair of docker containers to serve a flask page which generates a list of passwords (according to [XKCD rules](https://xkcd.com/936/ "Password Strength")) for users to select from
